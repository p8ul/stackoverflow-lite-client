export const questionTitle = (data) => {
	return `
    <div class="panel__heading bb" >
        <div class="mega-text hatch text-primary">${data.title}</div>                       
    </div>
`;   
};